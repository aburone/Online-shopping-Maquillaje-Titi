- @title = t.order.pos_to_wh.title

== slim :orders_list, locals: {edit_link: "/sales/transport/departures/pos_to_wh/order.o_id/add"}

form action=url("/transport/departures/pos_to_wh/new") method="post"
  == Rack::Csrf.tag(env)
  input type="submit" value="#{t.order.new}"
