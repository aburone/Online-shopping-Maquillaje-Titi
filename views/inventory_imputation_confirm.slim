- @title = t.sales.inventory_imputation.title(@order.o_id)

== slim :items_list, locals: {full_row: true, can_edit: false}

form.v_island_1#finish_packaging action=url("/inventory/inventory_imputation/#{@order.o_id}/finish") method="post"
  == Rack::Csrf.tag(env)
  input type="submit" value="Imputar items al stock"

/ form.v_island_1#finish_packaging action=url("/inventory/inventory_imputation/#{@order.o_id}/cancel") method="post"
/   == Rack::Csrf.tag(env)
/   input.ajax_confirm data-confirm_message="#{t.order.sure_to_cancel_load_order}" type="submit" value="#{t.actions.cancel}"
