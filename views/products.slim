-# coding: UTF-8
- full_row = true if full_row.nil?
- stock_col = false if stock_col.nil? unless stock_col == true
- sku_col = false if sku_col.nil? unless sku_col == true
- can_edit = false if can_edit.nil? unless can_edit == true
- edit_link = nil if edit_link.nil?
- can_filter = true if can_filter.nil?

== slim :products_list, locals: {can_edit: can_edit, full_row: full_row, stock_col: stock_col, sku_col: sku_col, edit_link: edit_link, can_filter: can_filter}
